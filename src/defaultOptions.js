const defaults = {
  host: document.location.host || '',
  username: '@Medium',
  moreBtn: '#moreBtn',
  wrapper: '.mediumWrap',
  targetBlank: true,
  defaultTemplate: true,
  postLimit: 5
};

export default defaults;
